// utils/aiDrip.js

// Placeholder AI drip function
// Your app expects this function to exist, even if it does nothing.
// You can later expand this into delayed message streaming.

export function addAiMessageDrip(chatId, text) {
  console.log("💧 Drip message for chat:", chatId);
}
